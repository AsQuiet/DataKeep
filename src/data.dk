@(NoBody)
struct Entity
{
    name : string;
    health : int;
    @(NoPrint)
    type : EntityType;
}

@(Print)
struct Player -> Entity
{
    playerID : string;
    playerName : string;
    charachterStrength : int;
}

enum EntityType
{
    Player,
    Zombie,
    Alien
}
